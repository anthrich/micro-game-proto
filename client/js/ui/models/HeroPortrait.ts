export default class HeroPortrait {
    id : number;
    name : string;
    url : string;

    constructor(id, name, url) {
        this.id = id;
        this.name = name;
        this.url = url;
    }
}